import styled from "styled-components";

export const LanguageDropdown = styled.div`
  display: flex;
  height: 50px;
  column-gap: 1rem;

  select {
    border: none;
    padding: 1rem;
  }

  img {
    height: 100%;
  }
`;
